// This file is just a re-export for easier pathing, as planned in your doc.
// We already defined the hook in FamilyContext.jsx, but this provides
// a clean import path: `import { useFamily } from '../hooks/useFamily'`

export { useFamily } from '../contexts/FamilyContext';